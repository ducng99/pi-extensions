import type { DefaultAgentDefinition } from "../types";

export default {
    name: "General",
    description: "General-purpose agent for delegating tasks",
    systemPrompt: "",
} satisfies DefaultAgentDefinition;
